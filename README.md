# Employee Management Backend

This repository contains the .NET 8 Web API for the Employee Management System. It uses Dapper and stored procedures to access SQL Server.

Run: dotnet restore; dotnet run in the src/EmployeeManagement.Api folder.
